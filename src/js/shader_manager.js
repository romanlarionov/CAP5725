
/*
 * Dynamically stitches together physically based shaders based on specified parameters.
 * Also keeps a global cache of shaders to avoid reloading whenever possible.
 * todo: figure out if I really want to do this..
 */
var ShaderManager = function() {

};

ShaderManager.prototype = {
    constructor: ShaderManager,

    create: function(type, smoothness) {

    }

};